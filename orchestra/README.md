# App stub réservée — **non** listée dans INSTALLED_APPS.
#
# Ne pas confondre avec le domaine métier « orchestre » (chat salon orchestra,
# MusicianProfile, etc.). Ce package Django était un placeholder vide.
#
# Si un jour un module dédié est nécessaire, réactiver ici + ajouter dans
# config.settings.base.INSTALLED_APPS.
