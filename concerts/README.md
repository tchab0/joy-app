# Legacy — ne pas utiliser

Anciens modèles `Lieu` / `Concert`. **Non** installés (`INSTALLED_APPS`).

La source de vérité est `events.Venue` / `events.Event`.
Ce dossier est conservé uniquement pour éviter une réintroduction accidentelle ;
ne pas importer ces modèles dans le code applicatif.
